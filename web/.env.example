# Empty = same-origin /api (Vite dev proxy → FastAPI :8000). Or set explicit base:
# VITE_API_BASE_URL=http://127.0.0.1:8000

# true = offline demo only (no POST /api/review). false = live API.
VITE_USE_MOCK=false
